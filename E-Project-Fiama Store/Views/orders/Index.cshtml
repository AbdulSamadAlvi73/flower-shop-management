@model IEnumerable<Fiama_Shop.Models.order>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutPageAdmin.cshtml";
}

<h2>Orders</h2>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.C_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.C_address)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.phone_no)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.C_date)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.C_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.C_address)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.phone_no)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.C_date)
        </td>
        @*<td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ordid }) |
            @Html.ActionLink("Details", "Details", new { id=item.ordid }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ordid })
        </td>*@
    </tr>
}

</table>
